**/.dockerignore
**/.env
**/.git
**/.gitignore
**/.project
**/.settings
**/.toolstarget
**/.vs
**/.vscode
**/.kiro
**/.brain
**/*.*proj.user
**/*.dbmdl
**/*.jfm
**/azds.yaml
**/bin
**/charts
**/docker-compose*
**/Dockerfile*
**/node_modules
**/npm-debug.log
**/obj
**/secrets.dev.yaml
**/values.dev.yaml
**/TestResults
LICENSE
README.md

# Test projects — not needed for any production Docker build
tests/
# But allow E2E tests (needed by e2e-tests.Dockerfile)
!tests/CodingAgentWebUI.E2ETests/

# Config — all runtime-mounted, not needed in build context
config/

docs/

# Helm chart — not needed in Docker build context
helm/

# GitHub Actions workflows — not needed in build context
.github/

# SQLite databases and credentials
*.sqlite3
*credentials*